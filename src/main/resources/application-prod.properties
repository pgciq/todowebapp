# Production Profile Configuration

# Application settings
app.name=Todo Web Application
app.version=1.0.0
app.environment=production

# Database connection settings (MySQL)
database.driver-class-name=com.mysql.cj.jdbc.Driver
database.url=jdbc:mysql://localhost:3306/todo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
database.username=todo
database.password=todo

# Database metadata
database.type=MySQL
database.name=todo
database.host=localhost
database.port=3306
database.schema=todo

# Logging settings
logging.level=INFO
logging.file=logs/todoapp-prod.log

# Production specific settings
debug.enabled=false
cache.enabled=true
